use_default_settings: true

general:
  debug: false
  instance_name: EvidenceMesh local search

search:
  safe_search: 1
  autocomplete: ""

server:
  secret_key: "local-development-only-change-before-public-deployment"
  limiter: false
  image_proxy: true

ui:
  static_use_hash: true

formats:
  - html
  - json
